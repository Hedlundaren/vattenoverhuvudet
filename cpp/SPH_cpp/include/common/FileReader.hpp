#pragma once

#include <string>

/// @brief //todo add brief description to FileReader
/// @author benjamin
class FileReader {
public:
    static const std::string ReadFromFile(std::string fileName);
};