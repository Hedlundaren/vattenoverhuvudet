#pragma once

#cmakedefine VOXEL_CELL_PARTICLE_COUNT @VOXEL_CELL_PARTICLE_COUNT@

#include "clParticle.hpp"

struct clVoxelCell {
    clParticle particles[VOXEL_CELL_PARTICLE_COUNT];
    cl_int particle_count;
};